# Data

- `raw/incercare v2.xlsx` is the final input workbook used by the clean replication workflow.
- `processed/` is reserved for regenerated processed files if a reviewer reruns selected stages.

The committed model-ready dataset is in `outputs/01_model_ready_data/`.
