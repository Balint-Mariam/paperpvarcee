# Processed Data

This folder is reserved for regenerated processed data if a reviewer runs custom intermediate steps. The committed final model-ready dataset is stored in `outputs/01_model_ready_data/`.
