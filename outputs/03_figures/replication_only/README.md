# Figure Replication Only

The main replication-only figure archive is stored in `outputs/06_replication_only/`.

This folder is reserved for any future figure exports that should be kept separate from the manuscript-ready main and appendix figures.
