# Old Outputs

This folder is for old or local intermediate outputs that are not part of the external-facing result set.

The local subfolder `local_untracked_intermediate_outputs/` is ignored by Git because it contains regenerated caches that were not part of the clean replication package.
